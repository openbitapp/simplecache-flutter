name: cache
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  gl_functional:
    path: ../gl_functional-flutter

dev_dependencies:
  pedantic: ^1.10.0
  test: ^1.16.0
